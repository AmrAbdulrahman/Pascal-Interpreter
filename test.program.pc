program Main {
  var x, c, s, cs: real;

  procedure square(n : integer) {
     var res : integer;
     var real1 : integer;
     res := n * n;
     return res;
  }

  procedure conditionalProcedure(n : integer) {
   if (true) {
     print('inside first if');
     if (false) {
        print('inside inner if');
        return 15;
     }
     print('after inner if');

     return n;
     print('definitely unreachable');
   } otherwise {
     var x : integer;
     return n + 2;
   }
  }


  // return expression on the fly
  // procedure cube(n : integer);
  // begin
  //  return n * n * n;
  // end;

  // function that calls a function
  // procedure compose(n : integer);
  // begin
  //  return square(n) + cube(n);
  // end;

  if (false)
    print('true amr hehe :D');
  else if (true)
    print('else if works!');
  otherwise
    print('false value found!');


  x := 2;
  // c := cube(x);
  s := square(x);
  //cs := compose(x);

  // argument as expression => function invokation
  // print('x =>', x, ' | sqr =>', s, ' | cube =>', c, ' | compose() =>', compose(x));
  // print('single quoted string');
  // print("double quoted string");
  // print("string with special \n\'\" characters");
  // print(conditionalProcedure(1));

  print('ss', s);

  return conditionalProcedure(8);
}
